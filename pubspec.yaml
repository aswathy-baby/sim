name: erp_solution
description: A new Flutter project.

publish_to: 'none' # Remove this line if you intend to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0  # Add this line
  # Add other dependencies below
  http: ^0.13.5  # Use the latest stable version as per pub.dev

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.0.17

flutter:
  uses-material-design: true

  # Add assets, fonts, etc., here


